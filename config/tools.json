{
  "tools": {
    "search_company_data": {
      "enabled": true,
      "student_note": "Turn ON to enable RAG: company files are searched and sent to the model. Turn OFF and no company_data is used.",
      "chunk_settings": {
        "max_chars": 800,
        "top_k": 3,
        "student_note": "max_chars = size of each text chunk when files are split. top_k = how many chunks are retrieved per search. Try smaller chunks or higher top_k and compare tokens in the Comparison lab."
      }
    },
    "calculator": {
      "enabled": true,
      "student_note": "Useful for prices, discounts, measurements, simple budgets, and technical calculations."
    },
    "current_datetime": {
      "enabled": true,
      "student_note": "Useful when the answer depends on today's date or time."
    },
    "create_support_ticket": {
      "enabled": false,
      "student_note": "Demo tool. Turn ON if your bot should simulate escalation to a human."
    }
  }
}
